const initialAccoutState = {
	id: null,
	icon: null,
	name: null,
	balance: null,
}

export const accountReducer = (state = initialAccoutState, action) => {
	switch (action.type) {
		default:
			return state
	}
}
