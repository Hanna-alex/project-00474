import {
	authorize,
	checkUserLoginExists,
	logout,
	register,
	updateUserData,
	getUserAccounts,
	getUserCategories,
} from './operations'

export const server = {
	authorize,
	checkUserLoginExists,
	logout,
	register,
	updateUserData,
	getUserAccounts,
	getUserCategories,
}
