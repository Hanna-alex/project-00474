import {
	authorize,
	checkUserLoginExists,
	logout,
	register,
	updateUserData,
} from './operations'

export const server = {
	authorize,
	checkUserLoginExists,
	logout,
	register,
	updateUserData,
}
